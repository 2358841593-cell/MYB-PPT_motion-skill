---
name: my-ppt-studio
description: 强约束流程的 PPT 生成工具。AI 必须按 5 步流程执行，禁止跳过任何步骤。
---

# my-ppt-studio

> 🎨 **AI 驱动的专业 PPT 生成工具**
>
> 从原始文案到精美演示文稿，5 步流程，全自动渲染

---

## 目录

- [视觉库总览](#视觉库总览)
- [操作流程](#操作流程)
- [核心特性](#核心特性)
- [案例展示](#案例展示)

---

## 视觉库总览

### 📊 导出组件清单（76 个可用组件）

所有组件从 `@/compositions/shared/visuals` 导出，可直接在 Remotion Composition 中使用。

| 类别         | 数量 | 组件列表                                                                                                                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **字体效果** | 13   | KeynoteText, GlowText, ShadowText, GradientText, StrokeText, NeonText, Retro3DText, SplitColorText, LetterPressText, MetallicText, OutlineShadowText, PerspectiveText, EmbossText |
| **抽象装饰** | 5    | FloatingDots, ConnectionLines, GlowingPoints, WaveLines, DecorativeGrid                                                                                                           |
| **几何装饰** | 6    | CircleRing, ConcentricCircles, HexagonGrid, DiamondShape, CornerBrackets, FloatingBlocks                                                                                          |
| **数据元素** | 5    | ProgressBar, MiniBarChart, StatCard, MiniLineChart, DataCardStack                                                                                                                 |
| **流程图**   | 9    | StepIndicator, Timeline, FlowArrows, ProcessNodes, CycleDiagram, Bracket, ContentFlow, HorizontalTimeline, Connectors                                                             |
| **设备框架** | 2    | DeviceFrame, DeviceCluster                                                                                                                                                        |
| **图表**     | 12   | PieChart, DonutChart, BarChart, HorizontalBarChart, LineChart, AreaChart, RadarChart, GaugeChart, ProgressBars, ScatterChart, DataCard, FunnelChart                               |
| **流程图库** | 12   | PyramidDiagram, OnionDiagram, HorizontalFlow, VerticalFlow, CycleDiagram2, TreeDiagram, OrgChart, MatrixDiagram, SwimlaneDiagram, ComparisonDiagram, StepsDiagram, ChevronDiagram |
| **形状装饰** | 12   | Arrow, DoubleArrow, CurvedArrow, BlockArrow, IconShape, Divider, ShapeBracket, Badge, Tag, PlaceholderBox, DecorLine, CornerMark                                                  |

### 🔤 字体效果库（13种）

| 组件名              | 效果       | 适用场景           |
| ------------------- | ---------- | ------------------ |
| `KeynoteText`       | 倒影效果   | 封面标题，高端感   |
| `GlowText`          | 发光效果   | 金句强调，暗色背景 |
| `ShadowText`        | 阴影效果   | 层次感，立体效果   |
| `GradientText`      | 渐变文字   | 品牌色，视觉冲击   |
| `StrokeText`        | 描边文字   | 艺术感，海报风格   |
| `NeonText`          | 霓虹灯效果 | 赛博朋克，科技感   |
| `Retro3DText`       | 3D立体     | 复古风格，层次感   |
| `SplitColorText`    | 双色分层   | 创意设计，对比效果 |
| `LetterPressText`   | 活字印刷   | 文艺风格，质感     |
| `MetallicText`      | 金属质感   | 高端商务，奢华感   |
| `OutlineShadowText` | 描边阴影   | 强调重点，醒目     |
| `PerspectiveText`   | 透视文字   | 动态感，空间感     |
| `EmbossText`        | 浮雕效果   | 立体效果，高级感   |

### 📊 图表库（12种）

| 组件名               | 用途     | 数据格式                        |
| -------------------- | -------- | ------------------------------- |
| `PieChart`           | 饼图     | `{value, color, label}[]`       |
| `DonutChart`         | 环形图   | `{value, color}[]`              |
| `BarChart`           | 柱状图   | `{value, color, label}[]`       |
| `HorizontalBarChart` | 横向柱图 | `{value, color, label}[]`       |
| `LineChart`          | 折线图   | `{value, label}[], color`       |
| `AreaChart`          | 面积图   | `{value, color}[]`              |
| `RadarChart`         | 雷达图   | `{labels[], values[], color}`   |
| `GaugeChart`         | 仪表盘   | `{value, max, color, label}`    |
| `ProgressBars`       | 进度条组 | `{label, value, color}[]`       |
| `ScatterChart`       | 散点图   | `{x, y, color}[]`               |
| `DataCard`           | 数据卡片 | `{label, value, change, color}` |
| `FunnelChart`        | 漏斗图   | `{label, value?, color}[]`      |

### 🔄 流程图库（12种）

| 组件名              | 用途       | 数据格式                 |
| ------------------- | ---------- | ------------------------ |
| `PyramidDiagram`    | 金字塔层级 | `{levels: [{label}]}`    |
| `OnionDiagram`      | 洋葱图层   | `{layers: [{label}]}`    |
| `HorizontalFlow`    | 横向流程   | `{steps: [{title}]}`     |
| `VerticalFlow`      | 纵向流程   | `{steps: [{title}]}`     |
| `CycleDiagram2`     | 循环图     | `{labels: []}`           |
| `TreeDiagram`       | 树形图     | `{root, children: []}`   |
| `OrgChart`          | 组织架构   | `{root, level1: []}`     |
| `MatrixDiagram`     | 矩阵图     | `{rows, cols, colors}`   |
| `SwimlaneDiagram`   | 泳道图     | `{lanes, steps, colors}` |
| `ComparisonDiagram` | 对比图     | `{left, right, colors}`  |
| `StepsDiagram`      | 步骤条     | `{steps, colors}`        |
| `ChevronDiagram`    | 箭头流程   | `{steps, colors}`        |

### 🎨 形状装饰库（12种）

| 组件名           | 用途     | 参数                                                                             |
| ---------------- | -------- | -------------------------------------------------------------------------------- |
| `Arrow`          | 箭头     | `direction, length, color, dashed`                                               |
| `DoubleArrow`    | 双向箭头 | `length, color`                                                                  |
| `CurvedArrow`    | 弯曲箭头 | `direction, size, color`                                                         |
| `BlockArrow`     | 块状箭头 | `direction, size, color`                                                         |
| `IconShape`      | 图标形状 | `type(circle/square/triangle/diamond/pentagon/hexagon/octagon/star/heart/cross)` |
| `Divider`        | 分割线   | `type(solid/dashed/dots/gradient), color`                                        |
| `ShapeBracket`   | 括号     | `type(round/square/curly), color`                                                |
| `Badge`          | 徽章     | `text, color`                                                                    |
| `Tag`            | 标签     | `text, color`                                                                    |
| `PlaceholderBox` | 占位框   | `width, height, color, label`                                                    |
| `DecorLine`      | 装饰线   | `type(wave/zigzag/double), color`                                                |
| `CornerMark`     | 角标     | `position(tl/tr/bl/br), color`                                                   |

### 🎯 智能可视化（通过 SmartVisual 组件）

`SmartVisual` 组件根据 `visualType` 自动选择渲染方式：

| visualType   | 效果     | 数据格式                     |
| ------------ | -------- | ---------------------------- |
| `pyramid`    | 金字塔   | `{levels: [{label}]}`        |
| `flow`       | 横向流程 | `{steps: [{title}]}`         |
| `cycle`      | 循环图   | `{labels: []}`               |
| `comparison` | 对比     | `{leftItems, rightItems}`    |
| `timeline`   | 时间线   | `{events: [{year, title}]}`  |
| `funnel`     | 漏斗图   | `{stages: [{label, value}]}` |
| `tree`       | 树形图   | `{root, children: []}`       |

---

## ⛔ 强制执行规则（所有使用者必须遵守）

### 修改任何幻灯片前必须完成的步骤

**Step 0: 读取上下文（不可跳过）**

必须读取以下文件：

```bash
# 1. 读取原始文案
cat <project>/sources/raw-content.txt

# 2. 读取当前规划
cat <project>/sources/script.md

# 3. 读取标准值库
cat /Users/myb/Desktop/PPT/motion-kit/src/styles.ts

# 4. 读取布局规范
cat /Users/myb/.opencode/skills/my-ppt-studio/references/split-layout-spec.md
```

**Step 1: 列出执行清单**

必须明确列出：

- 要修改的文件
- 要修改的具体内容
- 不能修改的内容（边界）

**Step 2: 等待用户确认**

**Step 3: 执行**

只修改清单中列出的内容，每项完成后勾选。

**Step 4: 验证**

```bash
# 渲染
node scripts/render-slide.mjs --project <project> --all

# 用图像分析验证每页
# 检查：文字列表是否存在、图片类型是否正确、内容是否匹配
```

**Step 5: 报告**

逐项报告修改和验证结果。

### 绝对禁止事项

| 禁止                   | 说明               | 正确做法                           |
| ---------------------- | ------------------ | ---------------------------------- |
| ❌ 硬编码 fontSize     | 如 `fontSize: 26`  | 使用 `currentStyle.bulletFontSize` |
| ❌ 硬编码 padding      | 如 `padding: 100`  | 使用 `currentStyle.contentPadding` |
| ❌ 擅自修改 visualType | 如 pyramid → onion | 必须用户确认                       |
| ❌ 删除 bullets        | 忽略原文案内容     | 必须保留                           |
| ❌ 不读规划直接改      | 凭记忆修改         | 先读取 raw-content.txt             |
| ❌ 跳过验证            | 修改后不检查       | 必须渲染+图像分析                  |
| ❌ 清单不执行          | 列了清单不按清单做 | 逐项勾选完成                       |

### 文件路径常量

| 文件     | 路径                                              | 用途      |
| -------- | ------------------------------------------------- | --------- |
| 原始文案 | `<project>/sources/raw-content.txt`               | 内容来源  |
| 规划文件 | `<project>/sources/script.md`                     | 当前状态  |
| 样式库   | `/Users/myb/Desktop/PPT/motion-kit/src/styles.ts` | 标准值    |
| 布局规范 | `references/split-layout-spec.md`                 | 布局约束  |
| 渲染脚本 | `scripts/render-slide.mjs`                        | 渲染命令  |
| 打包脚本 | `scripts/build-deck.mjs`                          | PPTX 生成 |

---

## 操作流程

### ⛔ 强制流程（不可跳过）

```
Step 1: 接收文案 → 仅接收，不执行任何操作
   ↓
Step 2: AI 手动编写规划 → 必须参考 references/layouts.md 选择布局
   ↓
Step 3: 视觉组件方案 → 每页必须匹配视觉库组件
   ↓
Step 4: 用户确认 → 必须等待用户明确同意
   ↓
Step 5: 渲染生成 → 用户确认后才能执行
```

### 禁止事项

| 禁止                          | 说明               |
| ----------------------------- | ------------------ |
| ❌ 禁止使用 parse-content.mjs | 正则切分不可控     |
| ❌ 禁止跳过步骤               | 必须 1→2→3→4→5     |
| ❌ 禁止提前渲染               | 必须等 Step 4 确认 |
| ❌ 禁止连续执行               | 每步需等待用户     |

### Step 1: 接收文案

**内容输入方式**：

支持两种方式输入内容，AI 会自动进行最适合 PPT 的文案拆解：

**方式 1: URL 读取**

提供网页 URL，自动读取并解析内容：

```bash
# 从 URL 读取
bun run scripts/parse-content.mjs --url https://example.com/article
```

**方式 2: 直接粘贴**

直接复制文案粘贴：

```bash
# 粘贴模式
bun run scripts/parse-content.mjs --paste
# 然后粘贴你的内容，按 Ctrl+D 结束
```

**AI 行为**：

1. 接收文案内容（URL 或粘贴）
2. 自动分析内容结构
3. 智能分页（5-30 页）
4. 存储到 `sources/raw-content.txt`
5. 输出确认信息
6. **停止，等待用户指令**

### Step 2: AI 手动编写规划

**AI 行为**：

1. 阅读 `references/layouts.md`
2. 理解文案内容
3. 为每页选择合适的布局
4. 输出逐页规划详情（内容、字体、视觉设计）
5. **停止，等待用户确认**

**规划输出格式**：

```
### Slide 01
- **内容**：封面页，标题"xxx"，副标题"xxx"
- **字体**：KeynoteText（倒影效果）
- **视觉设计**：✅ 有
  - 类型：glowing-points 霓虹光点
  - 位置：背景装饰
```

### Step 3: 视觉组件方案

**⛔ 强制确认流程（不可跳过）**

AI 在输出视觉方案时，**必须**对每页的视觉设计逐项确认以下 4 点：

```
### Slide XX 视觉确认
| 检查项 | 确认 | 说明 |
|--------|------|------|
| 尺寸合适 | ✅/❌ | 组件尺寸是否与布局匹配 |
| 内容清晰 | ✅/❌ | 文字是否可读，不拥挤 |
| 融入整体 | ✅/❌ | 视觉风格是否统一 |
| 位置正确 | ✅/❌ | 放置位置是否符合布局 |
```

**⚠️ 图文布局规范（必须遵守）**

参考 `references/split-layout-spec.md`：

| 规范项       | 要求                           | 说明                         |
| ------------ | ------------------------------ | ---------------------------- |
| 左右间距     | 50-60px                        | `gap: 50`                    |
| 左侧文字宽度 | 480-520px                      | 固定宽度，不是 flex          |
| 文字字体     | 24-26px                        | 列表项不要太小               |
| 对齐方式     | `alignItems: "center"`         | 左右两侧垂直居中             |
| 水平排列     | `justifyContent: "flex-start"` | 从左开始，不是 space-between |

**支持的 visual types（27 种）**：

**背景装饰类**（full-background 布局）：

- `floating-dots` - 浮动圆点
- `hexagon-grid` - 六边形网格
- `wave-lines` - 波浪线
- `glowing-points` - 霓虹光点
- `concentric-circles` - 同心圆

**内容可视化类**（sidebar-right 布局）：

- `pyramid` - 金字塔（520×380）
- `layers` - 层级堆叠（520×380）
- `flow` - 横向流程（500×120）
- `onion-diagram` - 洋葱图（320×320）
- `tree-diagram` - 树形图（420×280）
- `vertical-flow` - 垂直流程（320×400）
- `funnel-chart` - 漏斗图（360×320）
- `cycle-diagram` - 循环图（320×320）
- `chevron-diagram` - 箭头流程（500×70）
- `donut-chart` - 环形图（240×240）
- `bar-chart` - 柱状图（360×200）
- `line-chart` - 折线图（380×180）
- `radar-chart` - 雷达图（240×240）
- `gauge-chart` - 仪表盘（200×130）
- `comparison` - 对比列表（340×200）

**精简原则**：

- 只在真正需要时使用可视化
- `bullet-list` 和 `binary-comparison` 不需要额外可视化
- `hierarchical-layers` 布局才需要指定 visualType

**尺寸参考表**：

| 布局                | 可视区域   | 推荐组件尺寸                         |
| ------------------- | ---------- | ------------------------------------ |
| bullet-list         | 右侧 500px | flow, chevron-diagram                |
| hierarchical-layers | 右侧 550px | pyramid, onion-diagram, funnel-chart |
| three-columns       | 底部 700px | chevron-diagram                      |
| visual-content      | 左侧 400px | vertical-flow, tree-diagram          |

**数据格式**：

```yaml
# 金字塔/层级
visual:
  type: pyramid
  data:
    levels:
      - label: "第一层"
        sublabel: "描述"

# 流程图
visual:
  type: flow
  data:
    steps:
      - title: "步骤1"

# 洋葱图
visual:
  type: onion-diagram
  data:
    layers:
      - label: "核心层"

# 树形图
visual:
  type: tree-diagram
  data:
    root: "根节点"
    children: ["子节点1", "子节点2"]

# 循环图
visual:
  type: cycle-diagram
  data:
    labels: ["计划", "执行", "检查", "改进"]

# 漏斗图
visual:
  type: funnel-chart
  data:
    stages:
      - label: "访问"
        value: "1000"

# 图表类
visual:
  type: bar-chart
  data:
    items:
      - label: "Q1"
        value: 85
```

### Step 4: 用户确认

AI 必须等待用户明确同意（"确认"/"同意"/"继续"）

### Step 5: 渲染生成

```bash
# 渲染幻灯片
node scripts/render-slide.mjs --project <项目目录> --all

# 生成 PPTX
node scripts/build-deck.mjs --project <项目目录> --skip-render
```

---

## 核心特性

### ✨ 11 种视觉风格

| 风格             | 背景             | 适用场景        |
| ---------------- | ---------------- | --------------- |
| apple            | #0A0A0A 纯黑     | 科技/产品发布会 |
| tech             | #0F172A 深蓝     | 技术/开发文档   |
| bold-editorial   | #1A1A1A 深灰     | 创意/营销方案   |
| minimal          | #FAFAFA 浅灰白   | 商务/投资报告   |
| dark-atmospheric | #0D0D0D 纯黑霓虹 | 高端/沉浸体验   |
| corporate        | #1E293B 深蓝灰   | 企业/金融报告   |
| notion           | #FFFFFF 纯白     | SaaS/工具介绍   |
| blueprint        | #0A192F 工程蓝   | 技术/架构图     |
| chalkboard       | #2C3E50 深灰绿   | 教育/培训课件   |
| sketch-notes     | #FFF8DC 米黄     | 创意/头脑风暴   |
| scientific       | #F8FAFC 浅灰     | 学术/研究报告   |

### 🎯 14 种标准布局

**基础布局**：

- `title-hero` - 大标题居中，封面/章节页
- `quote-callout` - 金句/引用，带署名
- `bullet-list` - 要点列表，纯文字
- `key-stat` - 单个大数字，数据展示
- `split-screen` - 左右分屏，图文对比
- `three-columns` - 三栏并列
- `icon-grid` - 图标网格，功能展示

**信息图布局**：

- `binary-comparison` - A vs B 对比
- `hierarchical-layers` - 层级结构（金字塔）
- `circular-flow` - 环形循环，迭代流程
- `funnel` - 漏斗转化
- `timeline` - 时间线

### 布局选择决策表

| 内容类型          | 推荐布局            | 理由               |
| ----------------- | ------------------- | ------------------ |
| 封面/章节页       | title-hero          | 大标题，视觉冲击   |
| 金句/引用         | quote-callout       | 突出核心观点       |
| 简单列表（2-4条） | bullet-list         | 清晰易读           |
| 对比（A vs B）    | binary-comparison   | 左右对比，一目了然 |
| 流程/步骤         | hierarchical-layers | 顺序展示           |
| 层级结构          | hierarchical-layers | 金字塔/堆叠        |
| 循环流程          | circular-flow       | PDCA等             |
| 三组并列内容      | three-columns       | 平衡展示           |
| 关键数据          | key-stat            | 数字聚焦           |

### 布局优化规则（自动检测）

| 问题         | 自动解决方案             |
| ------------ | ------------------------ |
| 封面倒影重叠 | 副标题 marginTop >= 90px |
| 金字塔太小   | 默认 600×420 尺寸        |
| 三栏下方空   | 自动添加底部流程指示     |

### AI 生成时检查清单

1. **封面页**：如果使用 textEffect: keynote，确保内容简洁（标题+副标题最多2行）
2. **可视化页**：确保 visualData 内容精简，避免金字塔/流程图过于拥挤
3. **三栏页**：每栏 items 数量 >= 3，否则考虑用其他布局
4. **列表页**：bullets 数量 2-4 条，太少会显得空洞

---

## 案例展示

### 最终案例：Agent Skills 新范式

**路径**：`/Users/myb/Desktop/PPT/projects/agent-skills-final-demo/`

**规格**：

- **页数**：8 页
- **风格**：dark-atmospheric（纯黑 + 霓虹绿）
- **字体效果**：KeynoteText（封面倒影）、GlowText（金句发光）
- **布局**：title-hero × 1, quote-callout × 2, bullet-list × 3, three-columns × 1, hierarchical-layers × 1

**预览**：

| 页码 | 布局                | 字体效果         | 视觉组件       |
| ---- | ------------------- | ---------------- | -------------- |
| 01   | title-hero          | KeynoteText 倒影 | glowing-points |
| 02   | quote-callout       | GlowText 发光    | wave-lines     |
| 03   | bullet-list         | 标准             | 无             |
| 04   | bullet-list         | 标准             | 无             |
| 05   | hierarchical-layers | 标准             | pyramid        |
| 06   | three-columns       | 标准             | 底部流程指示   |
| 07   | bullet-list         | 标准             | 无             |
| 08   | quote-callout       | GlowText 发光    | hexagon-grid   |

---

## 质量检查

渲染完成后 AI 必须检查：

- [ ] 所有 PNG 文件已生成
- [ ] 每个 PNG 文件大小 > 30KB
- [ ] PPTX 文件已生成
- [ ] 布局与内容匹配（无白边、无布局错位）
- [ ] 字体效果已渲染（封面有倒影、金句有发光）

---

## 文件参考

| 文件                              | 说明                              |
| --------------------------------- | --------------------------------- |
| `README.md`                       | GitHub 开发者文档                 |
| `references/layouts.md`           | 布局参考（来自 baoyu-slide-deck） |
| `references/design-guidelines.md` | 设计指南                          |
| `scripts/render-slide.mjs`        | 渲染脚本，包含布局映射            |
| `scripts/build-deck.mjs`          | PPTX 生成脚本                     |

---

## 🎬 Motion Kit - PPT 动画化呈现

> PPT 制作完成后的最终呈现阶段

### 什么时候使用

用户对静态 PPT 满意后：

> "静态 PPT 已生成。是否需要动画化呈现？
>
> - MP4 视频版（自动播放，40秒）
> - HTML 交互版（用户控制翻页）"

---

### 设计理念

#### 与市面工具的本质区别

| 维度     | 传统 PPT 工具      | Motion Kit                     |
| -------- | ------------------ | ------------------------------ |
| 动画实现 | 预设模板，有限定制 | 代码驱动，完全可控             |
| 输出格式 | 仅 PPTX            | **MP4 视频 + HTML 交互播放器** |
| 图表动画 | 静态图表           | 竞速动画、扇区展开、数字滚动   |
| 背景效果 | 静态或简单过渡     | 呼吸光晕、浮动粒子、渐变流动   |
| 精度控制 | 不可控             | 帧级精度（30fps）              |
| 物理效果 | 线性过渡           | Spring 物理缓动                |

#### 核心优势

1. **专业级动画效果** - 对标 Apple Keynote
2. **开发者完全可控** - React + Remotion，代码即动画
3. **双输出格式** - MP4 自动播放 + HTML 交互演示
4. **帧级精度** - 30fps，每帧可控
5. **优雅 Spring 物理** - 真实物理感的缓动效果

---

### 动画效果库

#### 入场动画

| 效果       | Hook/组件             | 说明                                          |
| ---------- | --------------------- | --------------------------------------------- |
| 淡入上移   | `useSlideAnimation`   | 标题从下方淡入，带缩放                        |
| 依次入场   | `useStaggerAnimation` | 列表/卡片依次出现，间隔 8 帧                  |
| 标题入场   | `AnimatedTitle`       | 4 种效果：fadeUp, fadeIn, scaleIn, slideRight |
| 副标题入场 | `AnimatedSubtitle`    | 延迟 15 帧入场                                |
| 卡片入场   | `AnimatedCard`        | 依次入场，带缩放                              |
| 引号动画   | `AnimatedQuoteMark`   | 缩放 + 淡入                                   |

#### 图表动画

| 效果           | 组件             | 说明                              |
| -------------- | ---------------- | --------------------------------- |
| **竞速柱状图** | `RacingBar`      | 柱子依次生长，竞速效果，间隔 8 帧 |
| **饼图展开**   | `PieSliceReveal` | 扇区依次展开，间隔 6 帧           |

#### 背景动效

| 效果       | 组件                        | 说明                   |
| ---------- | --------------------------- | ---------------------- |
| 浮动粒子   | `FloatingDotsAnimated`      | 25+ 粒子随机浮动、闪烁 |
| 呼吸光晕   | `BreathingGlow`             | 中心光圈脉动，大小变化 |
| 同心圆脉动 | `ConcentricCirclesAnimated` | 5 圆环呼吸效果         |
| 渐变流动   | `GradientFlow`              | 色相缓慢旋转           |
| 角落括号   | `CornerBracketsAnimated`    | 4 角括号闪烁           |
| 底部线条   | `BottomLineAnimated`        | 宽度脉动               |
| 底部圆点   | `BottomDotsAnimated`        | 3 圆点依次闪烁         |

#### 数字动画

| 效果     | Hook           | 说明              |
| -------- | -------------- | ----------------- |
| 数字滚动 | `useCountUp`   | 从 0 滚动到目标值 |
| 光效脉冲 | `useGlowPulse` | 数字光晕脉动      |

---

### 导出能力

#### MP4 视频

```bash
npx remotion render src/index.ts Presentation out.mp4
```

- **帧率**：30fps
- **编码**：H.264
- **总时长**：8 页 × 5 秒 = 40 秒
- **适用**：视频分享、存档、自动演示

#### HTML 交互式播放器

```bash
npm run dev
# 访问 http://localhost:3000/presentation
```

- **交互控制**：用户决定何时进入下一页
- **键盘支持**：空格/右箭头下一页，左箭头上一页
- **自动暂停**：每页动画结束后自动暂停
- **适用**：现场演示、会议演讲

---

### 技术架构

#### Animation Hooks（6 个）

```typescript
// 入场动画
const { progress, opacity, scale, translateY } = useSlideAnimation(startFrame?);

// 依次入场
const { progress, opacity, scale, translateY } = useStaggerAnimation(index, delayPerItem?);

// 呼吸效果
const { scale, opacity } = useBreathingAnimation(speed?, minScale?, maxScale?);

// 浮动效果
const { translateY, translateX } = useFloatingAnimation(speed?, amplitude?, phaseOffset?);

// 数字滚动
const value = useCountUp(endValue, startFrame?, durationFrames?);

// 光效脉冲
const { glowOpacity, glowScale } = useGlowPulse(speed?, minOpacity?, maxOpacity?);
```

#### 核心配置

```typescript
// 优雅 Spring 物理配置
const ELEGANT_SPRING = {
  damping: 25, // 高阻尼 = 平滑
  stiffness: 80, // 适中刚度
};

// 幻灯片配置
const FPS = 30;
const ANIMATION_FRAMES = 90; // 3 秒动画
const HOLD_FRAMES = 60; // 2 秒停留
const SLIDE_FRAMES = 150; // 每页总帧数
```

#### Sequence 帧管理

```typescript
// 使用 Sequence 重置帧数上下文
<Sequence from={slideStart} durationInFrames={SLIDE_FRAMES} layout="none">
  <Slide />
</Sequence>
```

---

### 使用方式

#### 命令行渲染

```bash
# 渲染 MP4
npx remotion render src/index.ts Presentation out.mp4

# 启动交互播放器
npm run dev
```

#### 交互控制

| 操作          | 结果       |
| ------------- | ---------- |
| 空格键 / 点击 | 播放下一页 |
| 左箭头        | 返回上一页 |
| 动画结束      | 自动暂停   |

---

### 动画化流程

```
Step 1: 用户满意静态 PPT
    ↓
Step 2: 询问是否动画化
    ├── 否 → 结束
    └── 是 → 继续
    ↓
Step 3: 选择输出格式
    ├── MP4 视频版（自动播放）
    └── HTML 交互版（用户控制）
    ↓
Step 4: 渲染/启动
    ├── npx remotion render → out.mp4
    └── npm run dev → localhost:3000/presentation
```

---

### 动画文件结构

```
motion-kit/src/
├── hooks/
│   └── useSlideAnimation.ts        # 6 个 Animation Hooks
├── components/
│   ├── AnimatedSlide.tsx           # 6 个动画组件
│   └── PresentationPlayer.tsx      # HTML 播放器组件
├── compositions/
│   ├── Presentation.tsx            # 多页幻灯片容器
│   ├── charts/animations/
│   │   ├── RacingBar.tsx           # 竞速柱状图
│   │   └── PieSliceReveal.tsx      # 饼图展开
│   └── shared/visuals/
│       └── AnimatedBackgrounds.tsx # 8 个动态背景
└── app/presentation/
    └── page.tsx                    # HTML 播放器页面
```

---

### 预期动画效果

| 页面    | 布局         | 动画效果                                    |
| ------- | ------------ | ------------------------------------------- |
| 封面    | CoverHero    | 标题淡入+缩放，副标题延迟入场，浮动粒子背景 |
| 引言    | QuoteCallout | 引号缩放，文字淡入，同心圆脉动              |
| 饼图    | PieChart     | 标题入场，扇区依次展开，图例淡入            |
| 柱状图  | BarChart     | 标题入场，柱子竞速生长，数值滚动            |
| 三栏    | ThreeColumns | 标题入场，卡片依次入场，底部流程指示        |
| KeyStat | KeyStat      | 标题入场，数字滚动（0→目标），光晕脉动      |

---

## 版本历史

| 版本  | 日期       | 更新                                                               |
| ----- | ---------- | ------------------------------------------------------------------ |
| 2.0.0 | 2026-02-17 | Motion Kit: PPT 动画化呈现系统（MP4 + HTML 交互播放器）            |
| 1.5.0 | 2026-02-16 | 新增动画效果库（光影呼吸、文字光效、入场动画、过渡效果、图片动画） |
| 1.4.0 | 2026-02-16 | 添加强制执行规则（Step 0-5）、禁止事项表、文件路径常量             |
| 1.3.0 | 2026-02-16 | 新增图文布局规范，修复左右间距和字体大小问题                       |
| 1.2.0 | 2026-02-15 | VisualWrapper 支持 27 种 visual types，添加 Step 3 强制确认流程    |
| 1.1.0 | 2026-02-15 | 导出全部 76 个组件：12 图表 + 12 流程图 + 12 形状装饰              |
| 1.0.1 | 2026-02-15 | 修正组件清单：40 个导出组件（非 50+），移除不存在的组件            |
| 1.0.0 | 2026-02-15 | 初始版本：40 组件视觉库，11 种风格，14 种布局                      |
